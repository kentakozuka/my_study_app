<html>
	<h1><%= title %></h1>
	<table>
		<tr>
			<th>
				ID
			</th>
			<th>
				EXAM NAME
			</th>
		<tr>
		<% recordList.forEach(function(record) { if(record){ %>
			<tr>
				<td>
					<%= record.ID %>
				</td>
				<td>
					<%= record.EXAM_NAME %></p>
				</td>
			</tr>
		<% }}); %>
	</table>
</html>
